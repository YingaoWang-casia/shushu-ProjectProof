# Contributing

感谢你愿意贡献这个 Skill。这个项目的目标不是把简历写得更夸张，而是让真实经历更清楚、更可信、更能抗面试追问。

## 可以贡献什么

- 脱敏后的真实项目输入样例。
- 大厂面试项目深挖问题。
- 某个岗位方向的细节追问，例如 Agent、RAG、前端、后端、算法、数据工程、测试、DevOps、移动端。
- 危险简历表达和对应的安全改写。
- 手动 eval case：输入、必须识别的风险、必须追问的问题、可接受输出。
- 文档修正、结构优化、安装说明和使用体验反馈。

## 不要贡献什么

- 虚构经历、虚构指标、虚构公司规模、虚构上线状态。
- 未脱敏的简历、聊天记录、公司内部文档、代码截图、日志或数据。
- 能识别个人、学校、公司、客户或业务机密的信息。
- 鼓励用户造假的模板。

## 新增 eval 的格式

建议放到 `skills/bigtech-interview-project-upgrader/references/realistic-input-output-evals.md` 或单独开文件，并包含：

```text
用户输入：
...

必须命中的诊断点：
1. ...
2. ...

必须追问的问题：
1. ...
2. ...

不能写的表达：
...

安全改写方向：
...

补做后可写方向：
...
```

## 修改 references 的原则

- 一条规则只解决一个清晰问题，不要写泛泛的“提升表达质量”。
- 每个强表达都要回到证据：代码、日志、截图、接口文档、测试、评估集、bad case、日报或可复现实验。
- 对小厂项目保持尊重：不贬低真实经历，也不把 demo 包装成生产系统。
- 面试追问要能顺着用户项目继续深入，而不是堆八股。
- 模板中的 `TODO` 是占位符，不是未完成内容；新增模板时也请保持这种占位语义，并补充说明。

## 发布前检查

提交前建议完成：

```bash
find . -name '.DS_Store' -delete
rg -n "TODO|占位符说明" skills/bigtech-interview-project-upgrader/references -g "*.md"
```

并手动用 `manual-eval-suite.md` 至少跑 3 个样例，确认输出没有越界造假。
